# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# NOTE: players.json and stats.json are intentionally NOT ignored — the
# GitHub Action commits them back to the repo for the member portal to consume.

build_err.log
*.log
